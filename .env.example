# Add these values in Vercel (Project Settings → Environment Variables).
# Never use the VITE_ prefix: secrets must remain server-side.
# GOOGLE_MAPS_API_KEY requires Places API (New) and a Google Cloud billing account.
GOOGLE_MAPS_API_KEY=
GOOGLE_PLACE_ID=
BOOKING_ICAL_URL=
